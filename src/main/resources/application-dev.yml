server:
  servlet:
    context-path: /dmatnet-auth

spring:
  application:
    name: dmatnet-authentication
# reactive to database conectivity
  r2dbc:
    url: r2dbc:mysql://localhost:3306/dbauthentication
    username: root
    password: Tgb68789475
  sql:
    init:
      mode: always
    platform: mysql
  
  # JWT
jwt:
  expiration: 900000
  signup:
    url: /autenticacao/auth
  
  # Actuator
management:
  endpoint:
    health:
      show-details: always
  prometheus:
    enabled: true
  web:
    exposure:
      include: prometheus
  metrics:
    export:
      prometheus:
        enabled: true
    enable:
      jvm: true
    tags:
      application: dmatnet-authentication
  
  # Actuator - Info
info:
  app:
    name: @project.name@
    description: @project.description@
    version: @project.version@
    encoding: @project.build.sourceEncoding@
    java:
      version: @java.version@